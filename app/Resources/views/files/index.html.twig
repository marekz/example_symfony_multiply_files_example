{% extends 'base.html.twig' %}

{% block body %}
    <h1>Files list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>File</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for file in files %}
            <tr>
                <td><a href="{{ path('files_show', { 'id': file.id }) }}">{{ file.id }}</a></td>
                <td>{{ file.file }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('files_show', { 'id': file.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('files_edit', { 'id': file.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('files_new') }}">Create a new file</a>
        </li>
    </ul>
{% endblock %}
